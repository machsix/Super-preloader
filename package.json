{
  "name": "super-preloader",
  "version": "6.6.34",
  "description": "Super-preloader",
  "main": "Super_preloaderPlus_one_New.user.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "lint": "eslint Super_preloaderPlus_one_New.user.js dist/*.json .ci/*.js docs/**/*.js docs/**/*.js",
    "format": "prettier --ignore-path=.eslintignore --write Super_preloaderPlus_one_New.user.js dist/*.json .ci/*.js docs/**/*.js dist/*.json docs/**/*.{json,js,md}",
    "format:check": "prettier --ignore-path=.eslintignore --check Super_preloaderPlus_one_New.user.js dist/*.json .ci/*.js docs/**/*.js dist/*.json docs/**/*.{md,json,js}",
    "format:staged": "pretty-quick --staged --ignore-path=.eslintignore --write Super_preloaderPlus_one_New.user.js dist/*.json .ci/*.js docs/**/*.js dist/*.json docs/**/*.{json,js,md}",
    "test": "npm run lint && npm run format:check",
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "docs:publish": "npm run docs:build && .ci/gen_ghpage.sh"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint && npm run format:staged"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/machsix/Super-preloader.git"
  },
  "keywords": [
    "userscript"
  ],
  "author": "MachX",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/machsix/Super-preloader/issues"
  },
  "homepage": "https://github.com/machsix/Super-preloader#readme",
  "devDependencies": {
    "@vuepress/plugin-back-to-top": "1.0.2",
    "@vuepress/plugin-google-analytics": "1.0.2",
    "@vuepress/plugin-pwa": "1.0.2",
    "eslint": "6.1.0",
    "eslint-config-prettier": "6.0.0",
    "eslint-plugin-compat": "3.3.0",
    "eslint-plugin-json": "1.4.0",
    "eslint-plugin-prettier": "3.1.0",
    "husky": "3.0.0",
    "loglevel": "1.6.3",
    "prettier": "1.18.2",
    "pretty-quick": "1.11.1",
    "vuepress": "1.0.2"
  },
  "browserslist": [
    "Firefox ESR",
    "Chrome > 42"
  ],
  "dependencies": {}
}
