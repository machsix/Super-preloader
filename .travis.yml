language: python
python:
  - "2.7"
install:
    - npm install
script:
    - npm run test
deploy:
    - provider: script
      script: bash .travis/gen_ghpage.sh
      on:
        branch: master
    - provider: script
      script: bash .travis/gen_ghpage_dev.sh
      on:
        branch: dev